
    {% embed 'shared/_tabs.html.twig' %}
        {% block tabs %}
            {% embed 'shared/_tab.html.twig' with {route: 'app_proflie'} %}
                {% block content %}
                    Posty <span class="px-2 rounded-mb bg-gray-200 dark:bg-gray-700">{{ user.posts.count }}</span>
                {% endblock %}
                {% block path %}
                    {{ path('app_proflie', {id: user.id})}}
                {% endblock %}
            {% endembed %}

            {% embed 'shared/_tab.html.twig' with {route: 'app_proflie_follows', classes:'ml-5'} %}
                {% block content %}
                    Obserwowani <span class="px-2 rounded-mb bg-gray-200 dark:bg-gray-700">{{ user.follows.count }}</span>
                {% endblock %}
                {% block path %}
                    {{ path('app_proflie_follows', {id: user.id})}}
                {% endblock %}
            {% endembed %} 

            {% embed 'shared/_tab.html.twig' with {route: 'app_proflie_followers', classes:'ml-5'} %}
                {% block content %}
                    Obserwujący <span class="px-2 rounded-mb bg-gray-200 dark:bg-gray-700">{{ user.followers.count }}</span>
                {% endblock %}
                {% block path %}
                    {{ path('app_proflie_followers', {id: user.id})}}
                {% endblock %}
            {% endembed %}         
       
        {% endblock %}
    {% endembed %}
        <div class="mb-12"></div>