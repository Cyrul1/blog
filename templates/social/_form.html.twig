{{ form_start(form) }}
    <div>{{ form_errors(form) }}</div>

    <div>{{ form_label(form.title, 'Podaj tytuł!') }}</div>
    <div>{{ form_widget(form.title) }} </div>
    <div>{{ form_label(form.text, 'Napisz coś!') }}</div>
    <div>{{ form_widget(form.text, {'attr':{'style': 'color:grey'}}) }} </div>
    <div> <button type="submit">Wyślij!</button></div>

{{ form_end(form) }}